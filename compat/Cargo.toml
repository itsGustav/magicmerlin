[package]
name = "magicmerlin-compat"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# Add serde/serde_json once we start parsing OpenClaw status/snapshots.
